all:
  hosts:
    prometheus:
      ansible_host: prometheus.vpn.dev.cedi.dev
      ansible_user: root
  children:
    ipsec_gateways:
      hosts:
        gw1:
          ansible_host: gw1.vpn.dev.cedi.dev
          ansible_user: root
